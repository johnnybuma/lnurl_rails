<!DOCTYPE html>
<html>
  <head>
    <title>PRI2M</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= favicon_link_tag 'SYMBOLIC.png' %>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <script src="https://code.jquery.com/jquery-3.6.1.min.js" integrity="sha256-o88AwQnZB+VDvE9tvIXrMQaPlFFSUTR+nldQm1LuPXQ=" crossorigin="anonymous"></script>

    <script src="https://code.jquery.com/ui/1.13.2/jquery-ui.min.js" integrity="sha256-lSjKY0/srUM9BE3dPm+c4fBo1dky2v27Gdjm2uoZaL0=" crossorigin="anonymous"></script>

    <script src="https://cdn.jsdelivr.net/npm/sortablejs@latest/Sortable.min.js"></script>

    <style>
      <%= ".cont-#{current_user&.id}" %> {
          display: flex;
          justify-content: flex-end;
      }
      <%= ".msg-blue-#{current_user&.id}" %> {
          background-color: #007bff !important;
          color: white;
          padding: 100px;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row vh-100 overflow-auto">
        <%= render 'layouts/navigation' %>
        <div class="col d-flex flex-column h-sm-100" id="main-content">
          <main class="row overflow-auto">
            <div class="position-absolute top-0 start-30" style="z-index: 10001">
              <% if notice %>
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                  <%= notice %>
                  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
              <% end %>
              <% if alert %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                  <%= alert %>
                  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
              <% end %>
            </div>
            <%= yield %>
          </main>
          <footer class="row bg-light py-4 mt-auto">
            <div class="col"> I will leave this for now for future content here... </div>
          </footer>
        </div>
      </div>
    </div>
  </body>
</html>
